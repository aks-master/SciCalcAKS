---
- name: Deploy SciCalcAKS Application
  hosts: localhost
  become: yes
  tasks:
    - name: Pull latest Docker image
      command: docker pull aks00798/sci-calc-aks:latest

    - name: Stop and remove existing container if running
      command: docker rm -f sci-calc-aks
      ignore_errors: yes

    - name: Run the application container in interactive detached mode
      command: docker run -it -d --name sci-calc-aks aks00798/sci-calc-aks

